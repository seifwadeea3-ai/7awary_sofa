:root {
    --bg-dark: #0f0f0f;
    --glass: rgba(30, 30, 30, 0.85);
    --primary: #6c5ce7;
    --accent: #a29bfe;
    --bronze: #cd7f32;
    --silver: #00cec9;
    --gold: #fdcb6e;
    --danger: #ff7675;
    --text: #dfe6e9;
}

* { box-sizing: border-box; margin: 0; padding: 0; font-family: 'Cairo', sans-serif; outline: none; }

body {
    background-color: var(--bg-dark);
    color: var(--text);
    height: 100vh;
    overflow: hidden;
}

/* الشاشات */
.screen { display: none; height: 100%; width: 100%; }
.active-screen { display: flex; flex-direction: column; }

/* شاشة الدخول */
#auth-screen { justify-content: center; align-items: center; background: radial-gradient(circle at top, #2d3436, #000); }

.glass-card {
    background: var(--glass);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255,255,255,0.1);
    padding: 30px;
    border-radius: 20px;
    width: 90%;
    max-width: 400px;
    text-align: center;
    box-shadow: 0 10px 30px rgba(0,0,0,0.5);
}

.logo-text { color: var(--primary); font-size: 2em; margin-bottom: 10px; }
.input-group input {
    width: 100%; padding: 15px; margin: 10px 0;
    background: rgba(0,0,0,0.3); border: 1px solid #444;
    border-radius: 10px; color: white; font-size: 1em;
}

.btn-main {
    width: 100%; padding: 15px; border-radius: 12px; border: none;
    background: linear-gradient(45deg, var(--primary), var(--accent));
    color: white; font-weight: bold; cursor: pointer; margin-top: 10px;
    transition: transform 0.2s;
}
.btn-main:active { transform: scale(0.98); }
.btn-ghost { background: transparent; color: #888; border: none; margin-top: 15px; cursor: pointer; }

/* التطبيق الرئيسي */
.top-bar {
    padding: 15px; background: rgba(20,20,20,0.95);
    display: flex; justify-content: space-between; align-items: center;
    border-bottom: 1px solid #333;
}
.user-brief { display: flex; align-items: center; gap: 10px; }
.avatar-circle { width: 40px; height: 40px; background: #333; border-radius: 50%; display: flex; align-items: center; justify-content: center; }
.coins-display { background: #000; padding: 5px 12px; border-radius: 15px; color: var(--gold); border: 1px solid var(--gold); }

.content-area { flex: 1; overflow-y: auto; padding: 20px; padding-bottom: 80px; }
.tab-content { display: none; animation: fadeIn 0.4s ease; }
.active-tab { display: block; }

@keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }

/* كروت الغرف والمتجر */
.room-card, .store-card {
    background: var(--glass); padding: 15px; margin-bottom: 15px;
    border-radius: 15px; display: flex; align-items: center; gap: 15px;
    border: 1px solid #333; cursor: pointer; transition: 0.2s;
}
.room-card:active { background: #333; }
.room-icon { font-size: 24px; background: #222; width: 50px; height: 50px; display: flex; align-items: center; justify-content: center; border-radius: 12px; }
.room-info { flex: 1; }
.online-status { font-size: 0.8em; color: #00b894; }

.store-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(140px, 1fr)); gap: 10px; }
.store-card { flex-direction: column; text-align: center; }
.store-card button { background: #333; color: white; border: none; padding: 5px 15px; border-radius: 5px; margin-top: 10px; cursor: pointer; }

/* تأثيرات الرتب */
.bronze-glow { border-color: var(--bronze); box-shadow: 0 0 10px rgba(205, 127, 50, 0.2); }
.silver-glow { border-color: var(--silver); box-shadow: 0 0 10px rgba(0, 206, 201, 0.2); }
.gold-glow { border-color: var(--gold); box-shadow: 0 0 15px rgba(253, 203, 110, 0.3); }

/* الشات */
.chat-modal {
    position: fixed; top: 0; left: 0; width: 100%; height: 100%;
    background: var(--bg-dark); z-index: 1000; display: none; flex-direction: column;
}
.chat-header { padding: 15px; background: #1a1a1a; display: flex; justify-content: space-between; align-items: center; }
.chat-body { flex: 1; padding: 15px; overflow-y: auto; display: flex; flex-direction: column; gap: 10px; }
.msg { padding: 10px 15px; border-radius: 15px; max-width: 80%; width: fit-content; font-size: 0.9em; }
.msg.system { align-self: center; background: rgba(255,255,255,0.1); font-size: 0.8em; }
.msg.mine { align-self: flex-end; background: var(--primary); color: white; border-bottom-left-radius: 2px; }
.msg.theirs { align-self: flex-start; background: #333; border-bottom-right-radius: 2px; }

.chat-input-area { padding: 10px; background: #111; display: flex; gap: 10px; }
.chat-input-area input { flex: 1; padding: 10px; border-radius: 20px; border: none; background: #333; color: white; }
.chat-input-area button { width: 40px; height: 40px; border-radius: 50%; border: none; background: var(--primary); color: white; cursor: pointer; }

/* الناف بار السفلي */
.bottom-nav {
    position: fixed; bottom: 0; width: 100%; background: #151515;
    display: flex; justify-content: space-around; padding: 10px 0;
    border-top: 1px solid #333;
}
.nav-btn { background: none; border: none; color: #666; display: flex; flex-direction: column; align-items: center; gap: 5px; font-size: 0.8em; cursor: pointer; }
.nav-btn.active { color: var(--primary); }
.nav-btn .icon { font-size: 1.4em; }

/* البروفايل */
.profile-full { text-align: center; }
.avatar-large { width: 80px; height: 80px; margin: 0 auto 10px; background: #444; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 2em; }
.stats-row { display: flex; justify-content: center; gap: 20px; margin: 20px 0; }
.stat-box { background: rgba(0,0,0,0.3); padding: 10px 20px; border-radius: 10px; display: flex; flex-direction: column; }
.actions-list button { width: 100%; margin-bottom: 10px; padding: 12px; border-radius: 8px; border: none; background: #333; color: white; cursor: pointer; }
.actions-list .logout { background: var(--danger); color: white; }